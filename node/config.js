CONFIG = {
    project_path: '/Volumes/LocalDataHD/cjo20/Documents/chris/dev/DTRemoval/', // chris
    db: {
        name: 'dt_removal',
        user: 'root',
        password: 'root'
    }
};

exports.CONFIG = CONFIG;

// even though the CONFIG object is global, we return it here for use on the command line