default:
    context:
        parameters:
            #base_url: https://api.github.com
            base_url: http://private-384d2-amarapi.apiary-mock.com
    paths:
        features: features
        bootstrap: %behat.paths.features%/bootstrap
    extensions:
        Behat\MinkExtension\Extension:
            browser_name: chrome
            goutte:    ~
            selenium2: ~